# Ubuntu系统
绝对路径：从根目录开始，指定一个位置、文件
相对路径：从当前进程工作目录位置开始，指定一个位置、文件。
shell：命令解析器。默认运行在终端当中的程序(进程)。

## Linux目录结构
+ /：根目录，一般根目录下只存放目录，在Linux下有且只有一个根目录。所有的东西都是从这里开始。当你在终端里输入“/home”，你其实是在告诉电脑，先从/（根目录）开始，在进入到home目录。
+ /bin:/usr/bin：可执行二进制文件的目录。
+ cd .. :表示使用相对路径的方法
+ . ：表示当前目录
+ / ：表示目录分隔符或目录提示符
+ cd ../ ：上一级目录
+ /boot ：放置Linux系统启动时用到的一些文件，如Linux的内核文件。
+ 
